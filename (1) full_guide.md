# 🚀 Tensor Titan: Hackathon Master Guide

This document is your complete guide for the hackathon. It explains everything from the system architecture to the code, and gives you exact answers for the judging committee.

---

ctrl+shift+v

## 1. System Architecture (সিস্টেম কীভাবে কাজ করছে)

Tensor Titan মূলত ৪টি স্তরে (Layers) কাজ করে:

1. **Data Layer (`data_simulator.py`):** যেহেতু আমাদের কাছে রিয়েল সার্ভার নেই, তাই এটি ফেক (Simulated) ডেটা তৈরি করে। যেমন: GPU-র Temperature, Utilization, Memory. মাঝে মাঝে এটি ইচ্ছাকৃতভাবে কিছু "Anomaly" বা সমস্যা তৈরি করে (যেমন টেম্পারেচার হুট করে বাড়িয়ে দেওয়া) মডেলকে টেস্ট করার জন্য।
2. **ML Layer (`sensor_ai.py`):** এখানে দুটি Machine Learning মডেল আছে (Isolation Forest & Random Forest)। এগুলো ডেটা লেয়ার থেকে ডেটা নিয়ে অ্যানালাইসিস করে এবং বলে দেয় কোন GPU নষ্ট হওয়ার রিস্ক কতটুকু।
3. **Logic Layer (`scheduler_ai.py` & `cost_ai.py`):** 
   - **Scheduler:** রিস্ক স্কোর চেক করে। যে নোড সবচেয়ে সুস্থ (Healthy), সেখানে নতুন কাজ (Job) পাঠায়।
   - **Cost AI:** চেক করে কোন GPU কোনো কাজ ছাড়া বসে আছে (Idle) কিনা।
4. **UI Layer (`dashboard.py`):** Streamlit দিয়ে বানানো এই লেয়ারটি পেছনের সব হিসাব-নিকাশ আপনার স্ক্রিনে সুন্দর করে চার্ট আকারে দেখায় 

## 2. ড্যাশবোর্ডের ৩টি ট্যাব কী দেখাচ্ছে? (আপনার পাঠানো স্ক্রিনশটের ব্যাখ্যা)

### 📊 Tab 1: Cluster Health (আগের স্ক্রিনশট)
- এটি পুরো ক্লাস্টারের লাইভ স্ট্যাটাস।
- **কী দেখাচ্ছে:** প্রতিটি নোডের রিয়েল-টাইম টেম্পারেচার, মেমরি এবং **Risk Score**। Risk Score যদি ৭০ এর বেশি হয়ে যায়, তার মানে ওই GPU তে সমস্যা হতে পারে।

### ⚙️ Tab 2: Scheduler (আপনার পাঠানো ১ম নতুন স্ক্রিনশট)
- এটি হলো আমাদের Smart Task Manager।
- **Recent Job Assignments:** এখানে দেখাচ্ছে `Job-441 -> Node 4 | Score=0.7546`। এর মানে হলো, নতুন একটি কাজ (Job) সিস্টেমে আসায়, সিস্টেম চেক করে দেখেছে Node 4 সবচেয়ে বেশি সুস্থ এবং ফ্রি আছে (Score 0.75), তাই কাজটা Node 4 কে দেওয়া হয়েছে।
- **Migrations Triggered (183):** এর মানে হলো, সিস্টেম চলতে চলতে ১৮৩ বার এমন হয়েছে যে কোনো একটি GPU-র রিস্ক অনেক বেড়ে গিয়েছিল (Critical), তখন সিস্টেম নিজ দায়িত্বে ওই GPU থেকে কাজ সরিয়ে অন্য সুস্থ GPU তে পাঠিয়েছে (Migrate করেছে) যেন ডেটা লস না হয়!

### 💰 Tab 3: Cost & Idle (আপনার পাঠানো ২য় নতুন স্ক্রিনশট)
- এটি হলো আমাদের Money Saver।
- **Active GPUs (5) & Idle GPUs (0):** বর্তমানে ৫টি GPU-ই কাজ করছে। কোনোটা ফ্রি বসে নেই।
- **Hourly Cost ($12.5):** ৫টি GPU চালাতে ঘণ্টায় ১২.৫ ডলার খরচ হচ্ছে (প্রতিটি আড়াই ডলার করে)।
- **Monthly Waste ($0.0):** যেহেতু কোনো GPU অলস বসে নেই, তাই আপাতত কোনো টাকার অপচয় (Waste) হচ্ছে না। যদি কোনো GPU ৫ মিনিটের বেশি সময় ৫% এর নিচে লোড নিয়ে বসে থাকে, তখন সিস্টেম তাকে "Idle" ধরবে এবং অপচয়ের টাকা কাউন্ট করা শুরু করবে।

---

## 3. Committee Questions & Answers (টিচাররা যা জিজ্ঞেস করতে পারেন)

**Q1: তোমাদের কাছে তো রিয়েল GPU ক্লাস্টার নেই, তাহলে ডেটা কোথা থেকে আসলো?**
> **Answer:** "স্যার, হ্যাকাথনের জন্য আমরা `data_simulator.py` দিয়ে রিয়েল-ওয়ার্ল্ডের কাছাকাছি ডেটা জেনারেট করছি। তবে আমাদের সিস্টেম আর্কিটেকচার এমনভাবে বানানো যে, রিয়েল প্রোডাকশনে শুধু সিমুলেটরের জায়গায় Prometheus বা NVIDIA DCGM প্লাগ-ইন করে দিলেই এটি রিয়েল ডেটা নিয়ে কাজ করা শুরু করবে।"

**Q2: Machine Learning মডেলগুলো কোথায় ইউজ করেছো?**
> **Answer:** "স্যার, `sensor_ai.py` তে। আমরা Scikit-learn এর *Isolation Forest* ইউজ করেছি আনসুপারভাইজড অ্যানোমালি ডিটেকশনের জন্য (অস্বাভাবিকতা খোঁজার জন্য), আর *Random Forest* ইউজ করেছি ফেইলিউর প্রেডিক্ট করার জন্য। এই দুটো মিলে রিস্ক স্কোর তৈরি করে।"

**Q3: SchedulerAI কীভাবে কাজ করে?**
> **Answer:** "স্যার, এটি শুধু রাউন্ড-রবিন বা সিরিয়ালি কাজ দেয় না। এটি একটি Weighted Scoring Formula ইউজ করে। যে নোডের Risk Score কম, Available Memory বেশি এবং Temperature কম, তাকেই সিস্টেম সবচেয়ে বেশি স্কোর দেয় এবং নতুন কাজ সেখানেই অ্যাসাইন করে।"

**Q4: CostAI কীভাবে কস্ট কমাচ্ছে?**
> **Answer:** "স্যার, এটি প্রতি ১০ সেকেন্ড পর পর ব্যাকগ্রাউন্ডে চেক করে। যদি কোনো GPU ৫ মিনিটের বেশি সময় ৫% এর কম ইউটিলাইজেশনে থাকে, তখন সে তাকে Idle হিসেবে ফ্ল্যাগ করে। রিয়েল সিস্টেমে এটি ক্লাউড API (AWS/GCP) কে কল করে ওই GPU অফ করে দেবে। ড্যাশবোর্ডে আমরা দেখাচ্ছি ওই GPU অফ করলে ঠিক কত ডলার সেভ হবে।"

---

## 4. Improvements (ভবিষ্যতে কী ইম্প্রুভ করা যায়)
যদি টিচাররা জিজ্ঞেস করে "ভবিষ্যতে এটাকে আর কীভাবে বেটার করবে?", তখন বলবেন:
1. **Real Data Integration:** সিমুলেটরের বদলে রিয়েল সার্ভারের ডেটাবেস (Prometheus) কানেক্ট করা।
2. **Cloud API Triggering:** Idle GPU ডিটেক্ট করার পর ম্যানুয়ালি না করে, সিস্টেম নিজে থেকেই AWS/GCP তে কল দিয়ে সার্ভার স্কেল ডাউন (Scale-down/Turn off) করে দেবে।
3. **Reinforcement Learning:** বর্তমানে আমরা Scheduler এ একটি নির্দিষ্ট ফর্মুলা ইউজ করেছি। ভবিষ্যতে এখানে Reinforcement Learning ইউজ করা যেতে পারে, যেন সিস্টেম নিজের ভুল থেকে নিজে নিজে শেখে কোথায় কাজ দিলে সবচেয়ে ভালো পারফর্ম করবে।
