# 💻 Tensor Titan: Code Explanation Guide

এই ফাইলে `tensortitan` ফোল্ডারের প্রতিটি পাইথন কোড কীভাবে কাজ করছে, তা সহজ বাংলায় বুঝিয়ে দেওয়া হলো। হ্যাকাথনে জাজরা যদি কোড নিয়ে কোনো প্রশ্ন করে, তবে এখান থেকে উত্তর দিতে পারবেন।

---

## 1. `data_simulator.py` (The Fake Data Generator)
যেহেতু আমাদের রিয়েল সার্ভার নেই, তাই এই ফাইলটি ফেক GPU মেট্রিক্স তৈরি করে।
- `NUM_NODES = 5`: আমাদের সিস্টেমে মোট ৫টি ভার্চুয়াল GPU আছে।
- `ANOMALY_CHANCE = 0.10`: মানে হলো ১০% সম্ভাবনা আছে যে কোনো একটি GPU তে সমস্যা বা অস্বাভাবিকতা (Anomaly) দেখা দেবে। 
- **কীভাবে কাজ করে?** এটি একটি লুপ চালায় এবং প্রতিটি GPU এর জন্য `temperature`, `utilization`, `memory_used` জেনারেট করে। যদি Anomaly ট্রু (True) হয়, তবে টেম্পারেচার অনেক বাড়িয়ে দেয় (যেমন 88-98°C) যেন AI সেটা ধরতে পারে।

---

## 2. `sensor_ai.py` (The Brain / Machine Learning)
এটি আমাদের সিস্টেমের ব্রেইন। এখানে Scikit-Learn ব্যবহার করে দুটি মডেল বানানো হয়েছে।
- **Isolation Forest:** এটি আনসুপারভাইজড লার্নিং (Unsupervised Learning)। এর কাজ হলো ডেটার মধ্যে কোনটা "স্বাভাবিক" আর কোনটা "অস্বাভাবিক", তা খুঁজে বের করা।
- **Random Forest:** এটি সুপারভাইজড লার্নিং। এটি আগে থেকে ট্রেইন করা ডেটা দেখে বলে দেয় কোন GPU নষ্ট হওয়ার সম্ভাবনা কতটুকু।
- **`predict_risk` ফাংশন:** এই ফাংশনটি নতুন ডেটা নিয়ে মডেলকে দেয় এবং একটি **Risk Score (0 থেকে 100)** তৈরি করে। 
  - Score < 40 হলে `Normal`
  - Score < 70 হলে `Warning`
  - Score >= 70 হলে `Critical`

---

## 3. `scheduler_ai.py` (The Task Manager)
এর কাজ হলো কোন GPU তে কোন কাজ (Job) দেওয়া হবে, তা ঠিক করা।
- **`heapq` (Priority Queue):** পাইথনের এই লাইব্রেরিটি ব্যবহার করা হয়েছে যেন সবচেয়ে ইম্পরট্যান্ট কাজগুলো সবার আগে অ্যাসাইন হয়।
- **`_score_node` ফাংশন:** এটি একটি নোডকে মার্কস দেয়। যে নোডের Risk কম, মেমরি বেশি খালি আছে এবং টেম্পারেচার কম, তাকে বেশি স্কোর দেয়।
- **`assign_job` ফাংশন:** যে নোড সবচেয়ে বেশি স্কোর পায়, কাজটা তাকে দিয়ে দেয়।
- **`check_migration` ফাংশন:** যদি কোনো নোডের Risk Score ৭৫ (Critical Risk) এর উপরে চলে যায়, তবে এটি সাথে সাথে ওয়ার্নিং দেয় যে এখান থেকে কাজ সরিয়ে অন্য কোথাও (Migrate) নিতে হবে।

---

## 4. `cost_ai.py` (The Money Saver)
এটি খেয়াল রাখে যেন কোনো GPU অকারণে বসে না থাকে।
- **Threading:** এটি `threading` ব্যবহার করে ব্যাকগ্রাউন্ডে সবসময় চলতে থাকে।
- **লজিক:** `IDLE_THRESHOLD = 5.0` এবং `IDLE_MINUTES_REQUIRED = 5`। এর মানে হলো, যদি কোনো GPU টানা ৫ মিনিট ধরে ৫% এর কম ইউটিলাইজেশন দেখায়, তবে সিস্টেম তাকে **Idle (বসে আছে)** হিসেবে মার্ক করে।
- **`get_cost_summary` ফাংশন:** এটি হিসাব করে কয়টি GPU বসে আছে এবং তার জন্য প্রতি ঘণ্টায় কত ডলার অপচয় (Waste) হচ্ছে।

---

## 5. `database.py` (The Storage)
এটি `SQLite` ডেটাবেস ব্যবহার করে সব রেকর্ড সেভ করে রাখে।
এখানে ৪টি টেবিল আছে:
1. `gpu_metrics`: টেম্পারেচার ও ইউটিলাইজেশনের ডেটা রাখে।
2. `predictions`: AI এর তৈরি করা রিস্ক স্কোরগুলো সেভ করে।
3. `jobs`: কোন কাজ কোন GPU তে দেওয়া হলো, তার রেকর্ড রাখে।
4. `idle_log`: কোন GPU কতক্ষণ বসে ছিল, তা লিখে রাখে।

---

## 6. `dashboard.py` (The UI / Streamlit)
এটি ইউজার ইন্টারফেস (User Interface)। পেছনের সব হিসাব-নিকাশ এটি স্ক্রিনে দেখায়।
- `st.set_page_config`: ওয়েবসাইটের টাইটেল ও লেআউট সেট করে।
- `st.tabs`: ৩টি আলাদা ট্যাব (Cluster Health, Scheduler, Cost & Idle) তৈরি করেছে।
- `plotly` (px, go): সুন্দর পাই-চার্ট (Pie Chart) এবং বার-চার্ট (Bar Chart) বানানোর জন্য এই লাইব্রেরি ইউজ করা হয়েছে।
- `time.sleep(5)` & `st.rerun()`: ফাইলের একদম শেষে থাকা এই কোডটি ড্যাশবোর্ডকে প্রতি ৫ সেকেন্ড পর পর অটো-রিফ্রেশ করে, যার কারণে আপনি স্ক্রিনে রিয়েল-টাইম ডেটা দেখতে পান।

---

## 7. `main.py` (The Starter)
এটি হলো সিস্টেমের সুইচ!
এটি একবার রান করলেই পুরো সিস্টেম স্টার্ট হয়ে যায়:
1. ডেটাবেস তৈরি করে (`init_db`)
2. মডেল ট্রেইন করে (`train_models`)
3. কস্ট স্ক্যানার চালু করে (`start_scanner`)
4. শুরুতে কিছু ডামি জব (Job) কিউ-তে দিয়ে দেয়। 

---
**💡 টিপস:** হ্যাকাথনে যখন টিচাররা আসবেন, তখন `dashboard.py` এবং `sensor_ai.py` এর কোডগুলোর দিকে ফোকাস করে বোঝাবেন। কারণ UI এবং AI মডেলই হলো এই প্রজেক্টের মেইন অ্যাট্রাকশন!
